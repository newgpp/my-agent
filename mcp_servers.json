{
  "servers": {
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "${FS_ALLOWED_DIR_1}",
        "${FS_ALLOWED_DIR_2}"
      ]
    },
    "tavily": {
      "command": ".venv/bin/python",
      "args": [
        "servers/tavily_mcp_server.py"
      ]
    }
  }
}
